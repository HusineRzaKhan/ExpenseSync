// Mobile configuration
// Default API URL points to emulator's host. Adjust when running on a real device.
const API_URL = 'http://10.0.2.2:5000/api'; // Android emulator: 10.0.2.2 => host localhost

export default {
  API_URL,
};
