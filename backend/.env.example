PORT=5000
# Example MongoDB Atlas connection string. Replace <username>, <password> and <dbname>.
# Use the format (SRV):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.6c7y1cc.mongodb.net/<dbname>?retryWrites=true&w=majority
# Or for local MongoDB:
# MONGO_URI=mongodb://localhost:27017/expensesync

# IMPORTANT: Do NOT commit real credentials. Copy this file to `.env` and fill values.
MONGO_URI=

# A strong secret for signing JWTs
JWT_SECRET=
